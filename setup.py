from setuptools import setup

setup(
    name='safeimport',
    version='1.0',
    description='A python context manager util that lets you use modules from parent directory in a script.',
    author='apri-me',
    packages=['safeimport'],
    install_requires=[
    ],
)
